/CLAUDE.md

# build output (ships as a release asset, not in git)
main.js

# dependencies
node_modules/

# OS / editor
.DS_Store
.claude/

# local env (per-machine vault path for build deploy); .env.example is tracked
.env
